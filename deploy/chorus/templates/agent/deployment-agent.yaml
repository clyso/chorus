{{- if .Values.agent.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "chorus.fullname" . }}-agent
  labels:
    {{- include "chorus.labels" . | nindent 4 }}
    app.kubernetes.io/component: agent
spec:
  replicas: {{ .Values.agent.replicas }}
  selector:
    matchLabels:
      {{- include "chorus.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: agent
  template:
    metadata:
      labels:
        {{- include "chorus.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: agent
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/agent/config.yaml") . | sha256sum }}
        {{- if include "chorus.createCredentialsSecret" . }}
        checksum/secret: {{ include (print $.Template.BasePath "/secrets/secret.yaml") . | sha256sum }}
        {{- end }}
        {{- if .Values.agent.config.metrics.enabled }}
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: "9090"
        {{- end }}
        {{- with .Values.agent.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "chorus.serviceAccountName" . }}
      {{- with .Values.agent.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      terminationGracePeriodSeconds: 10
      containers:
        - name: agent
          image: "{{ .Values.agent.image.repository }}:{{ .Values.agent.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.agent.image.pullPolicy }}
          {{- with .Values.agent.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          env:
            - name: CFG_REDIS_ADDRESS
              value: {{ include "chorus.redisAddresses" . | quote }}
            - name: CFG_REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "chorus.redisSecretName" . }}
                  key: {{ include "chorus.redisSecretKey" . }}
            {{- if and (not .Values.redis.enabled) .Values.externalRedis.sentinel.masterName }}
            - name: CFG_REDIS_SENTINEL_MASTERNAME
              value: {{ .Values.externalRedis.sentinel.masterName | quote }}
            {{- end }}
            {{- if .Values.dynamicCredentials.enabled }}
            - name: CFG_STORAGE_DYNAMICCREDENTIALS_MASTERPASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "chorus.dynamicCredentialsSecretName" . }}
                  key: {{ include "chorus.dynamicCredentialsSecretKey" . }}
            {{- end }}
            {{- if not .Values.agent.config.url }}
            - name: CFG_URL
              value: {{ printf "http://%s-agent.%s.svc.cluster.local:%d" (include "chorus.fullname" .) .Release.Namespace (int .Values.agent.service.port) | quote }}
            {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.agent.config.port }}
              protocol: TCP
            {{- if .Values.agent.config.metrics.enabled }}
            - name: metrics
              containerPort: 9090
              protocol: TCP
            {{- end }}
          livenessProbe:
            tcpSocket:
              port: {{ .Values.agent.config.port }}
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            tcpSocket:
              port: {{ .Values.agent.config.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
          {{- with .Values.agent.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: config
              mountPath: /bin/config/config.yaml
              subPath: config.yaml
            {{- if or .Values.existingCredentialsSecret (include "chorus.createCredentialsSecret" .) }}
            - name: credentials
              mountPath: /bin/config/override.yaml
              subPath: config.yaml
            {{- end }}
      volumes:
        - name: config
          configMap:
            name: {{ include "chorus.fullname" . }}-agent
        {{- if or .Values.existingCredentialsSecret (include "chorus.createCredentialsSecret" .) }}
        - name: credentials
          secret:
            secretName: {{ include "chorus.credentialsSecretName" . }}
        {{- end }}
      {{- $nodeSelector := include "chorus.nodeSelector" (list .Values.agent.nodeSelector .Values.global.nodeSelector) }}
      {{- if $nodeSelector }}
      nodeSelector:
        {{- $nodeSelector | nindent 8 }}
      {{- end }}
      {{- $tolerations := include "chorus.tolerations" (list .Values.agent.tolerations .Values.global.tolerations) }}
      {{- if $tolerations }}
      tolerations:
        {{- $tolerations | nindent 8 }}
      {{- end }}
      {{- $affinity := include "chorus.affinity" (list .Values.agent.affinity .Values.global.affinity) }}
      {{- if $affinity }}
      affinity:
        {{- $affinity | nindent 8 }}
      {{- end }}
{{- end }}
