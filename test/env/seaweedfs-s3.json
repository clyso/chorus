{
  "identities": [
    {
      "name": "{{ .Name }}",
      "credentials": [
        {
          "accessKey": "{{ .AccessToken }}",
          "secretKey": "{{ .SecretToken }}"
        }
      ],
      "actions": ["Admin", "Read", "Write", "List", "Tagging"]
    }
  ]
}
